# Development environment (library consumers should `pip install hybrid-semantic-cache`).
# Equivalent to: pip install -e ".[demo,dev]"
faiss-cpu>=1.7.0
numpy>=1.24.0
sentence-transformers>=2.2.0

# demo extra
fastapi
uvicorn
google-generativeai

# dev extra
pytest>=7.0
build
twine
